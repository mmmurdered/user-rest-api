spring:
  data:
  datasource:
    url: jdbc:mysql://localhost:3306/user_rest
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
    hikari:
      maximum-pool-size: 20
      pool-name: HikariCP-1
      auto-commit: true
  jpa:
    hibernate:
      ddl-auto: create

ageOfRegistration: 18